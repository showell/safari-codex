================================================================================
SAFARI -> CODEX : research notes and cold-agent brief
Written 2026-08-28 by a Claude session running in ~/showell_repos/codex-zig-ladder.
Long-form companion (same material, prettier):
    http://143.244.172.148:9100/notes/porting-the-safari-to-codex.md
================================================================================

THE BRIEF (read this first if you have no memory of any of it)

  Steve wants the "Safari" driving screensaver -- a ~3,400-line Zig program at
  ~/showell_repos/angry-gopher/games/driving/wasm/ that computes a 3D scene and
  writes a flat draw-command buffer for a thin JS blitter -- ported to CODEX, a
  self-hosted functional language developed at ~/showell_repos/NewRepository
  (the project is called Cobblestone; the language is Codex). The Zig version
  stays completely intact and keeps running on lynrummy.com/driving; this is a
  parallel port, not a migration. The port is verified by transpiling the Codex
  to Zig with `codexzig` (a transpiler this box already builds and trusts) and
  comparing the resulting executable's stdout against stdout from a Zig probe
  that imports the REAL game modules unmodified -- so the hand-written Zig is
  the oracle. The Codex-to-Zig toolchain, its limits, and a large body of hard-
  won context live in ~/showell_repos/codex-zig-ladder (read its README and
  PRIORITIES.md; Steve will point you at the rest). This directory
  (~/showell_repos/safari-codex) is new and empty apart from these notes and a
  working spike; it is not yet a git repo and has no memory infrastructure yet.
  Steve knows all of this and will help you get oriented -- ask him.

--------------------------------------------------------------------------------
0. STATUS AT HANDOFF
--------------------------------------------------------------------------------

Nothing is ported. What exists is:
  - this file
  - spike/spike2.codex        a hand-ported camera fragment in Codex that RUNS
  - spike/probe_camera.zig    the Zig probe that produced its gold numbers

The feasibility question is answered YES and the loop was executed end to end.
No decisions have been made about where the port lives or how it is structured.
Three open decisions are listed at the bottom.

--------------------------------------------------------------------------------
1. THE MACHINE AND THE TOOLS
--------------------------------------------------------------------------------

  COBBLESTONE_ROOT   /home/steve/showell_repos/NewRepository   (the Codex tree)
  the game           /home/steve/showell_repos/angry-gopher/games/driving/wasm/
  the ladder         /home/steve/showell_repos/codex-zig-ladder
  the transpiler     /home/steve/showell_repos/codex-zig-transpiler

  codexzig binary    ~/showell_repos/codex-zig-ladder/native/codexzig
                       (built 2026-08-26 from the u51 pin; see native/PROVENANCE)
                     ~/showell_repos/codex-zig-transpiler/generated/local/codexzig
                       (newer, 2026-08-28; either works)

  bundler            ~/.local/pwsh/pwsh -NoProfile -File \
                       $COBBLESTONE_ROOT/build/bundle-app.ps1 -Src X -Out Y
                     (pwsh IS on this box but is NOT on PATH)

  zig                /home/steve/zig-0.16.0/zig   (0.16.0)
                     NB 0.16 removed std.io.getStdOut(). In probes use
                     std.debug.print, or std.fs.File.stdout().writer(&buf).

THE LOOP, verbatim, as run:

  pwsh -NoProfile -File $COBBLESTONE_ROOT/build/bundle-app.ps1 \
       -Src spike2.codex -Out spike2-unit.codex        # ~1 s, 3 deps + root
  native/codexzig < spike2-unit.codex 2> spike2.zig    # 0.064 s  (zig on STDERR)
  zig build-exe spike2.zig -O ReleaseFast              # ~1 s
  ./spike2

Under five seconds, and IT NEVER BOOTS A GUEST. This is nothing like the
ladder's usual QEMU cadence -- iteration here is cheap. (The ladder's "one
compute job at a time" rule is about QEMU guests; none of this touches them.)

Note the calling convention: codexzig reads Codex on stdin and writes the ZIG
TO STDERR. Its stdout is diagnostics.

A "bundle" is one file containing every cited chapter plus the root chapter,
with cited chapters renamed `Chapter: Foreword--Console` style. Cites resolve
by name inside the file. Our own chapters can therefore be concatenated into
one bundle -- but bundle-app.ps1 only resolves cites that live inside
COBBLESTONE_ROOT, so a multi-chapter port will need our own small bundler (the
ladder has cite-resolution machinery: cite_resolve.py, check_bundles.py,
bundle_reach.py -- look before writing a new one).

--------------------------------------------------------------------------------
2. WHAT WAS PROVEN (this actually ran; not a sketch)
--------------------------------------------------------------------------------

spike/probe_camera.zig imports the REAL, UNMODIFIED wasm/camera.zig and
wasm/geom.zig and prints:

  focal 685.510900        px 582.826660        py 341.130650
  camfocal 574.115360     rider.right -3.797166   rider.forward 7.199412
  drop 4.500000

spike/spike2.codex is a hand port of camera.FOCAL and camera.project plus a
Taylor sine lifted from `Gpu chapter DeviceMath`. Transpiled and run, it grades
itself against those literals and prints:

  focal      ok
  px         ok          <- tolerance 1e-4 on a 582.82666 pixel
  sin1       ok
  cos100     ok
  tight-px   ok

Conclusion: Codex real arithmetic transpiles and runs, and a hand port of the
camera agrees with the Zig to four decimals on a screen pixel.

(To re-run: the probe needs `wasm` to resolve as a relative import -- the spike
used a symlink, `ln -s ~/showell_repos/angry-gopher/games/driving/wasm wasm`,
beside probe_camera.zig.)

--------------------------------------------------------------------------------
3. THE THREE WALLS
--------------------------------------------------------------------------------

3.1 THE BLOCKER: the zig plug cannot convert between Real and Integer.

  error: zig plug: no emitter for real-to-int
  error: zig plug: no emitter for real-from-int

  ZigEmitter.codex has 69 builtin-emitter entries (54 distinct names); of the
  whole real-conversion family only `bits-to-real-approx` is present. Everything
  else falls through to the generic refusal at ZigEmitter.codex:1145. This is a
  HOLE, not a considered refusal (the considered refusal is `show` on a Real,
  which says so).

  Consequences:
   - The port stops at module three. pond / camera / geom / critter / paint /
     safari_critter / guard_rail need no conversion. sky / mountains / world /
     rider / tower / tree / truck / render / safari all do (colour ramps, the
     dusk clock, the flipbook index, segment counts). That is ~80% of the game.
   - `Gpu chapter DeviceMath` CANNOT BE TRANSPILED AT ALL today: real-sin calls
     dm-reduce, which calls real-from-int. The chapter that gives Codex its sine
     is dark to the zig arm. That is a finding on its own.

  The fix looks small and unambiguous. Bare metal is cvttsd2si / cvtsi2sd
  (codex/compiler/Emit/X86_64Builtins.codex:1663-1679) -- truncate toward zero,
  x86 "integer indefinite" (INT64_MIN) on NaN or overflow. Zig's @intFromFloat
  truncates the same way but is UB out of range, so the honest emission is a
  small prelude helper with three guards, not a bare cast.

3.2 `show` on a Real is REFUSED, deliberately.

  @compileError("zig plug: show on a Real needs __real_to_text, which this plug
  does not have"). The emitter explains: std.fmt would agree with bare metal on
  some values and not others, so it refuses rather than guess. That is right;
  do not touch it.

  So a transpiled Codex program CANNOT PRINT A FLOAT. Two ways around it:
   (a) THE GRADING PATTERN (what the spike does, and what Codex's own
       codex/test/lib/device-math.codex does): the Zig side emits the gold, the
       Codex side computes and prints ok / BAD against a stated tolerance. No
       float ever crosses the wire. WORKS TODAY, no plug change.
   (b) SCALED INTEGERS, once real-to-int lands: print
       real-to-int (x * 1000.0) and plain-diff the dumps. Much better for a
       thousand-value frame dump, where "BAD" alone tells you nothing.
  Start with (a); move to (b) when the plug allows.

3.3 Codex `Real` is f64 in the plug; the game is f32.

  ZigEmitter.codex:297 and :328 map every RealTy to f64 whatever width the type
  asked for. There is no way to make the port compute in f32, so EVERY
  comparison is a tolerance comparison, forever. See section 6 for the full
  investigation -- it turns out this is a house-wide convention, not a zig quirk,
  and it does not matter to the picture.

--------------------------------------------------------------------------------
4. THE SEAMS (four oracles, all already cut by the game's architecture)
--------------------------------------------------------------------------------

SEAM 1 -- the leaf math. geom.zig and camera.zig are pure functions of scalars.
  A probe walks a table of inputs and prints outputs. This is the one that ran.

SEAM 2 -- the static data. pond.zig is NOTHING BUT data (an outline, a bank, six
  ducks, three colours); so is most of world.zig's route table and the
  tree/tower dimension tables. Dump and compare exactly. This is the right FIRST
  port because it has no arithmetic -- it tests the harness, not the math.

SEAM 3 -- the draw-command buffer. THE MONEY SEAM, and already serialisable:
  paint.zig writes [tag][color][nPoints][x f32][y f32]... and
  paint.frameWords() hands the whole frame over as []const u32. A Zig probe
  decodes one frame to text; the Codex port -- which will RETURN a list of
  commands rather than write a buffer, being pure -- prints the same list. Tags
  and colours compare exactly, coordinates with tolerance. When this matches for
  a frame, THE SCENE CORE IS PORTED.

SEAM 4 -- the fold over time. safari.zig is the only module with mutable state;
  everything upstream is pure. The whole ride is state -> state.

  *** THE HAZARD IN SEAM 4 ***
  rider.decide() picks the lean by BINARY SEARCH over a simulated path -- twelve
  probes, each a float comparison. A 1e-7 f64-vs-f32 difference will eventually
  flip one, and from there the two rides are on different trajectories. A long
  trace comparison WILL go red and it will NOT mean the port is wrong.
  So do not compare trajectories. Compare ONE STEP AT A TIME FROM A SHARED
  STATE: the Zig probe dumps state N and state N+1; the Codex side is fed state
  N and must produce N+1. Drift cannot accumulate and a failure points at one
  step and one module instead of at frame 4000. Same for the truck. For a
  whole-ride check, check INVARIANTS (on the road, no loop, sunset monotone) --
  which is what games/driving/test/test_model.ts already does for the TS.

--------------------------------------------------------------------------------
5. SCOPE: WHAT NOT TO PORT
--------------------------------------------------------------------------------

- The blitter and the rasterizer. wasm/blitter.js and native/raster.zig are the
  display; the architecture already declares them the far side of the seam. The
  draw-command buffer is the contract. (Same call the project made for
  Delivery: zig solver, TS display.)
- cat_frames.zig and emoji_frames.zig BY HAND -- 615 KB of baked polygons, and
  they are GENERATED (ops/bake_cat runs the real TS drawing code through a
  recorder). Teach the baker a Codex emitter and the data ports itself. Late;
  nothing needs it until the cat crosses.
- A browser build through zig. The transpiled zig is a hosted program (std,
  ArrayListUnmanaged, a 4 GB bump region from page_allocator); wasm32-freestanding
  is not close. BUT SEE SECTION 7 -- there is a WASM PLUG in the Codex tree and
  that may be the real door.

THE BUMP HEAP, before it bites you: the emitted prelude reserves ~4 GB and
bump-allocates; NOTHING IS EVER RECLAIMED. Every list, every record, every
frame. A pure port that rebuilds the scene per frame will exhaust that in a few
thousand frames and @panic("oom"). Not a blocker -- it means ONE PROBE PROCESS
PER SHORT HORIZON, not one process driving the whole route. Design for it now.

--------------------------------------------------------------------------------
6. THE f32 / f64 INVESTIGATION (Steve asked; this is the whole answer)
--------------------------------------------------------------------------------

6.1 Where the game's f32 comes from: ONE PLACE.
  paint.zig packs each coordinate into a single u32 word --
      buf[cursor] = @bitCast(p.x);
  -- and blitter.js reads them back through
      new Float32Array(mem.buffer, base, len / 4)
  THE DRAW-COMMAND BUFFER IS THE f32. Everything upstream (ScreenPt, geom.Vec3,
  RiderPt, every camera constant) is f32 SO THAT that bitcast is free. That is
  the whole causal chain. Nothing in the physics or the look asks for it.

  Two consequences worth saying out loud:
   - The TypeScript original computed all of this in f64, because JS has no
     other number. The zig port NARROWED it. A Codex port in Real (f64) is
     arguably closer to the original than the zig is.
   - THE WIDTH DOES NOT CHANGE THE PICTURE. Measured this session, same probe,
     camera.zig+geom.zig copied and s/\bf32\b/f64/g:

         quantity            f32 (the game)     f64
         FOCAL               685.510900         685.511043
         projected x         582.826660         582.826656
         projected y         341.130650         341.130663
         camFocal(0.5,0.2)   574.115360         574.115499

     Worst gap 1.4e-4 on a 685-pixel value = 2e-7 relative = f32 epsilon to the
     digit. A ten-thousandth of a pixel on a 960x600 canvas. The width is
     invisible to the product and visible only to the oracle. If the wire is
     ever wanted back, round to f32 AT PUSH TIME -- the narrowing belongs at the
     seam, which is where the zig already puts it.

  So: the game is not constrained to f32. Its BUFFER is.

6.2 What the plugs do -- and this is NOT a zig-plug quirk.
  ZigEmitter.codex drops the width at both type sites:
      zig-let-annot   is RealTy (w) (m) -> ": f64"     (line 297)
      emit-zig-type   is RealTy (w) (m) -> "f64"       (line 328)
  (w) and (m) are bound and never read, with no comment either place. And the
  binary-operator table collapses five opcodes onto one symbol (1270-1273):
      is IrAddNum | IrAddVec | IrAddRealApprox | IrAddRealTrapping
         | IrAddRealSaturating -> "+"
  Three semantic collapses in one line: f32 computed as f64, TRAPPING silently
  not trapping, SATURATING silently not saturating.

  The neighbours do the same. The C# plug -- the best crib, and the one their
  own release gate runs -- is character-for-character identical
  (codex/plugs/csharp/CSharpEmitterExpressions.codex:1021-1024). The WASM plug
  states it as policy (codex/plugs/wasm/WasmEmitter.codex:682-684):
      "Every real on this target is carried as f64 bits in an i64 slot,
       IrNumLit included, so there is one width to reinterpret and no f32 case
       to separate."
  So: REAL IS A DOUBLE, HOUSE-WIDE. With ~50 plugs that is a convention, not an
  oversight, and a PR quietly making zig the exception would be a bad citizen.

6.3 EXCEPT THAT BARE METAL IS NOT A DOUBLE.
  The reference compiler picks the opcode by type
  (codex/compiler/IR/LoweringTypes.codex:182-185):
      is OpAdd -> ... else if is-real-approx-type ty then IrAddRealApprox ...
  and codex/compiler/Emit/X86_64.codex:2774 emits ADDSS for it -- single-
  precision add in an XMM low lane. The reference computes f32 in f32; every
  plug computes it in f64. A Codex program written in `Real approximate` gets
  DIFFERENT NUMBERS from the reference compiler than from any plug.

  Upstream is not casual about the width. codex/test/ops/real-approx-modes.codex
  opens: "THE WIDTH IS THE WHOLE RISK HERE. [...] Run the f32 values through the
  f64 constants and the exponent read is nonsense, the guard never fires, and
  saturating quietly stops saturating." Its gold pins a saturating clamp to one
  bit: 2139095039 (largest finite single), not 2139095040 (+inf).

6.4 Why nobody has tripped over it: THE f32 TESTS CANNOT BUILD THROUGH THE PLUG.
  Both of upstream's f32 ops tests were transpiled this session; neither
  compiles:
      real-approx.zig:       no emitter for to-real-approx
                             no emitter for from-real-approx
      real-approx-modes.zig: no emitter for real-approx-from-int
                             no emitter for real-approx-to-bits
                             no emitter for to-real-approx-saturating
  There is no way to get an f32 value INTO or OUT OF a transpiled program, so
  the arithmetic collapse has never been observable from the zig arm. THE
  MISSING HOLE HIDES THE WRONG ANSWER. Fixing the conversions without touching
  the arithmetic makes the divergence visible -- an argument for doing them in
  that order, on purpose, and saying so.

6.5 Would a ZigEmitter32 be crazy? The framing is off, and usefully so.
  A separate plug is right when the difference is a property of the TARGET.
  Width is not: it is a property of the TYPE IN THE SOURCE PROGRAM. `Real` and
  `Real approximate` can appear in the same chapter, in the same function.
  ZigEmitter32 would be a 4,000-line fork to change two arms and would still be
  wrong for any program using both widths -- which is most programs that use f32
  at all, since you narrow at a seam and compute in the middle.

  There is also nothing to configure: the information is already at every site
  and is thrown away twice --
    - the declared CodexType carries RwF32 (the (w) those two arms ignore);
    - the IR opcode carries it independently (IrAddRealApprox vs IrAddNum).
  Two independent channels, both already threaded to the emitter. The opposite
  of a plug-level flag.

  What honouring the width would cost, from outside:
    - emit-zig-type + zig-let-annot: RealTy RwF32 -> "f32". Two lines.
    - LITERALS ARE THE REAL WORK. IrNumLit emits @as(f64, @bitCast(@as(i64,n))),
      an f64 bit pattern; zig rejects that where an f32 is wanted, so the
      num-lit sites need the context type. The emitter already carries
      peer-type patch-ups for exactly this shape (ZigEmitter.codex:1567, 1657,
      2185) -- hardcoded to f64. That is where the difficulty lives; do not
      price it without trying it.
    - cx_bits_to_real_approx in the prelude deliberately widens f32->f64 with a
      comment saying "Real is f64 here". That comment becomes false; the helper
      becomes identity.
    - The conversion builtins are missing regardless -- same PR as real-to-int.
    - MODES ARE A BIGGER, SEPARATE JOB; do not take them on. The exponent guards
      differ per width (their own comment says so). But there is a cheap and
      strictly-better move: make trapping and saturating REFUSE instead of
      silently degrading. Silent wrong answer -> compile error, about an hour,
      cannot make anything worse, and it is the plug's own idiom (refusing
      `show` on a Real is the same move).

6.6 Recommended order, if any of this is taken up:
    1. Add the f64 conversions (real-to-int, real-from-int). Unblocks the port
       AND unblocks Gpu chapter DeviceMath, which is dark to zig today.
    2. Make trapping and saturating refuse. Cheapest correctness win on the board.
    3. ASK about the width; do not PR it. It is a convention across ~50 plugs and
       their own gate follows it, so the first artifact is a question with a
       repro, not a patch. The repro is free: their own real-approx.codex, which
       the zig arm cannot build, plus the addss at X86_64.codex:2774 that says
       what the answer should have been.

  AND FOR THE PORT ITSELF: none of this is on the critical path. Port in Real
  (f64) and grade against the zig f32 with a stated tolerance. 1e-4 of a pixel
  is a fine gate, and it is exactly the width gap -- so the oracle stays honest
  about what it is and is not measuring.

--------------------------------------------------------------------------------
7. THE WASM PLUG (the interesting lead; UNTESTED)
--------------------------------------------------------------------------------

codex/plugs/wasm/ exists: WasmEmitter.codex is 2,882 lines, "Transpiles Codex IR
to WebAssembly text format", and the directory ships browser-shim.html,
build-page.ps1, build-spark.ps1, build-designer.ps1, wasm-e2e.ps1, run.ps1 and a
test/ dir. There are ~50 plugs in that tree (ada angular arm64 ... wasm wgsl zig)
and this one targets exactly the environment the game already lives in.

This was found late and NOTHING about it has been tested: not whether it can
carry a program this size, not what it supports, not whether its output could
feed the existing wasm/blitter.js. If "the Safari, in Codex, in the browser" is
a finish line worth having, that is the door to knock on -- and it is the one
plug whose f64 policy costs nothing, because the wire narrows to f32 at the
buffer anyway.

Caveat carried over from section 6: this plug shares the house f64 convention
and states it explicitly, so the same tolerance story applies there.

--------------------------------------------------------------------------------
8. SUGGESTED BREAKDOWN (sizes are hand-written zig lines, baked data excluded;
   3,374 lines total across wasm/*.zig)
--------------------------------------------------------------------------------

PHASE 0 -- the harness, on pond alone (49 lines, no arithmetic).
  Build the four steps: bundle, transpile, build, grade. Decide where the port
  lives. Get one "ok" per duck. The point is that the LOOP is committed and
  repeatable before any math is in question.

PHASE 1 -- the leaf math: geom (110) + camera (67), with sine and sqrt CITED
  from `Gpu chapter DeviceMath` rather than rewritten. Seam 1 with a real input
  table. Ends with the projection agreeing on a few hundred points. Good place
  to stop and show Steve something.

PHASE 2 -- the plug PR: real-to-int / real-from-int with the three out-of-range
  guards, plus the DeviceMath-is-dark finding written up. Unblocks everything
  below and is worth sending on its own merits. Half a day, most of it ceremony.
  (The ladder's outbound protocol is: a finding reaches upstream ONLY via a PR
  carrying a compiler-backlog.md / plugs-backlog.md row -- see
  NewRepository/contrib/README.md and the ladder's findings/README.md.)

PHASE 3 -- the scene furniture: sky (100), mountains (169), tree (174),
  tower (186), guard_rail (108), critter (55), safari_critter (52), and paint
  (199) as a command-list builder. Each is seam 2 or seam 3 on one object.
  Highly parallel, low risk, mechanical.

PHASE 4 -- the world and the ride: world (329), rider (262), truck (305),
  gaze (106), cat (187). The one-step oracle carries this phase. The binary
  search in decide() is the hard part of the whole project.

PHASE 5 -- the frame: render (661) and the state fold from safari (255). Ends at
  a full draw-command match for a frame, then a PNG.

Phases 0-1 are a session. Phase 2 is a session. Phases 3-5 are the week. For
scale: the original TypeScript -> Zig port took about a day, but that was
imperative to imperative; this one changes paradigm (pure functional target, no
mutable buffers) and pays for the plug holes.

--------------------------------------------------------------------------------
9. OPEN DECISIONS FOR STEVE
--------------------------------------------------------------------------------

1. WHERE DOES THE PORT LIVE? This directory (~/showell_repos/safari-codex) was
   created for the handoff, but the probes want a relative @import of the game's
   ../wasm/*.zig, which argues for games/driving/codex/ inside angry-gopher.
   A symlink solves it either way. Nothing in either location is built by
   ops/build_safari_wasm, so gopher's deploy is untouched regardless.
2. DO WE SPEND PHASE 2 ON THE PLUG, OR WORK AROUND IT? The workaround is a
   fixed-point port in integer milli-units, which is how every Codex foreword
   math chapter already works (Geodesic, Matrix3, Complex -- all integers, no
   reals). That would be a MORE IDIOMATIC Codex port and a MUCH LESS FAITHFUL
   one, and it would rewrite every formula. Recommendation: fix the plug.
3. IS "A PNG FROM CODEX, BESIDE THE ZIG'S" THE FINISH LINE? -- or is it the wasm
   plug and a live page? The PNG is reachable with the existing native
   rasterizer (games/driving/native/main.zig already writes one frame per
   segment to games/driving/snap/native/segNN.png). The wasm plug is unexplored.

--------------------------------------------------------------------------------
10. POINTERS
--------------------------------------------------------------------------------

  The prettier version of everything above, with the same evidence:
      http://143.244.172.148:9100/notes/porting-the-safari-to-codex.md
  (source: ~/showell_repos/essay-repl-server/notes/porting-the-safari-to-codex.md)

  The game's own design doc, and it is a GOOD one -- read it before porting
  anything, especially "the one durable architecture idea: rider-relative, no
  global coordinates" and the look doctrines:
      ~/showell_repos/angry-gopher/games/driving/README.md
      ~/showell_repos/angry-gopher/games/driving/native/README.md

  The Codex-to-zig context, and the reason any of this is possible:
      ~/showell_repos/codex-zig-ladder/README.md      (start here)
      ~/showell_repos/codex-zig-ladder/PRIORITIES.md  (the live direction)
      ~/showell_repos/codex-zig-ladder/findings/README.md
      ~/showell_repos/codex-zig-transpiler/README.md  (what codexzig is)

  Codex language material worth reading before writing Codex:
      NewRepository/CLAUDE.md                     (read FIRST)
      NewRepository/codex/foreword/gpu/DeviceMath.codex   (real-sin/cos/sqrt)
      NewRepository/codex/test/lib/device-math.codex      (the grading pattern)
      ~/showell_repos/codex-zig-transpiler/samples/arith.codex  (a whole program)
      Gotcha that has cost builds twice: Codex refuses a multi-line application
      (CDX1070) -- use let..in.

  One thing NOT written down anywhere else: the ~50 plugs live at
  NewRepository/codex/plugs/ and the zig one is codex/plugs/zig/ZigEmitter.codex
  (3,906 lines). Read the top of that file before editing it; the ladder's
  memory set has a "BEFORE EDITING ZigEmitter" entry for a reason.

================================================================================
